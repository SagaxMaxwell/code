# Python 工具模块

- `stdlib_tools.ipynb`: Python 工具教程，包含 `math`、`cmath`、`decimal`、`fractions`、`calendar`、定时调度器 `sched` 和 APScheduler。
- `pydantic_tools.ipynb`: Pydantic 数据校验工具教程，包含订单模型、批量校验、JSON 序列化和错误整理。
