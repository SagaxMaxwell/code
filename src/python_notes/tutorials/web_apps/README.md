# Gradio 和 Streamlit 教程

- `apps.ipynb`: Gradio、Streamlit 入门，包含组件、状态、缓存和运行方式。

## 运行

项目环境已包含 Gradio 和 Streamlit。Gradio 可直接运行 notebook 代码；Streamlit 示例保存为 `.py` 后运行：

```bash
uv run streamlit run app.py
```
